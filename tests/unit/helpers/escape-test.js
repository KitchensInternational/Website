
import { escape } from 'kitchens-international/helpers/escape';
import { module, test } from 'qunit';

module('Unit | Helper | escape');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = escape([42]);
  assert.ok(result);
});

