
import { backgroundImage } from 'kitchens-international/helpers/background-image';
import { module, test } from 'qunit';

module('Unit | Helper | background image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = backgroundImage([42]);
  assert.ok(result);
});

