
import { odd } from 'kitchens-international/helpers/odd';
import { module, test } from 'qunit';

module('Unit | Helper | odd');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = odd([42]);
  assert.ok(result);
});

