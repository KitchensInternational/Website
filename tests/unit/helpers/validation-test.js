
import { validation } from 'kitchens-international/helpers/validation';
import { module, test } from 'qunit';

module('Unit | Helper | validation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validation([42]);
  assert.ok(result);
});

